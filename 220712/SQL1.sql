-- Crtl + Shift + Enter 는 모든 문장을 실행
-- Crtl + Enter 는 한문장만 실행
SHOW DATABASES;

USE world;

SHOW TABLES;

SELECT 3 % 2;

SELECT "Hello World";   -- "", '' 둘다 문자열 표현 문자열의 합연산은 불가능하다.

SELECT 10 <> 1;  -- 여기서는 결과값이 1 이 true 0이 false. 